meta {
  name: alerts
  seq: 2
}

auth {
  mode: inherit
}

docs {
  # Inventory Alerts

  Persisted low-stock, expiring-soon, and expired inventory alert lifecycle.

  Acknowledging an alert marks it as read. Resolution occurs only when the underlying inventory condition no longer applies.
}
