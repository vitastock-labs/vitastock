meta {
  name: inventory
  seq: 5
}

auth {
  mode: inherit
}

docs {
  # Inventory

  Workspace-scoped drug master, stock movement, activity, summary, and alert endpoints.

  All requests require an active authenticated workspace membership. Workspace IDs are derived from the session and must not be supplied by clients.
}
