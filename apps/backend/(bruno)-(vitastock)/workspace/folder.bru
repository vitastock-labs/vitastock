meta {
  name: workspace
  seq: 2
}

auth {
  mode: inherit
}

docs {
  # Workspace

  Membership, invitations, access control, and inventory alert settings for the authenticated workspace.

  Workspace IDs are never accepted from the client. Every operation is scoped through the authenticated membership.
}
